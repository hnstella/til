# bootstrap-datepicker

```javascript
/* bootstrap-datepicker init */
function fn_bootstrap_datepicker(obj, format) {
  obj.datepicker({
    format: format,
    autoclose: true,
    todayHighlight: true,
    weekStart: 0,
    todayBtn: 'linked',
    language: 'kr'
  });
}
```
